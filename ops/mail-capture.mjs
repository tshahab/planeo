import { createServer } from "node:http";
const messages = new Map();
createServer((request, response) => { if (request.method === "GET" && request.url === "/messages") { response.setHeader("content-type", "application/json"); response.end(JSON.stringify([...messages.values()])); return; } if (request.method !== "POST" || request.url !== "/messages") { response.writeHead(404).end(); return; } let body = ""; request.on("data", (chunk) => { body += chunk; }); request.on("end", () => { const key = request.headers["idempotency-key"]; if (typeof key === "string" && !messages.has(key)) messages.set(key, { id: key, receivedAt: new Date().toISOString(), ...JSON.parse(body) }); response.writeHead(202).end(); }); }).listen(8025, "0.0.0.0", () => console.info("Mail capture listening on :8025"));
