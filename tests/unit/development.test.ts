import{createHmac}from"node:crypto";import{describe,expect,it}from"vitest";import{normalizeDevelopmentEvent,verifyDevelopmentWebhook}from"../../src/lib/development";describe("development providers",()=>{it("verifies GitHub and GitLab signatures",()=>{const body='{"ok":true}',github=new Headers({"x-hub-signature-256":`sha256=${createHmac("sha256","secret").update(body).digest("hex")}`}),gitlab=new Headers({"x-gitlab-token":"secret"});expect(verifyDevelopmentWebhook("GITHUB","secret",body,github)).toBe(true);expect(verifyDevelopmentWebhook("GITLAB","secret",body,gitlab)).toBe(true);expect(verifyDevelopmentWebhook("GITHUB","wrong",body,github)).toBe(false)});it("normalizes provider contracts",()=>{expect(normalizeDevelopmentEvent("GITHUB",{repository:{id:1,full_name:"acme/app",html_url:"https://example.test/acme/app"},pull_request:{id:2,title:"Fix WEB-12",body:"",html_url:"https://example.test/pr/2",updated_at:"2026-01-01T00:00:00Z",base:{ref:"main"}}})?.kind).toBe("PULL_REQUEST");expect(normalizeDevelopmentEvent("GITLAB",{object_kind:"merge_request",project:{id:1,path_with_namespace:"acme/app",web_url:"https://example.test/acme/app"},object_attributes:{id:2,title:"WEB-12",url:"https://example.test/mr/2",updated_at:"2026-01-01T00:00:00Z"}})?.kind).toBe("MERGE_REQUEST")})});
