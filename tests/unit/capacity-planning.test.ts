import{describe,expect,it}from"vitest";import{calculateCapacity,weekKey}from"@/lib/capacity-planning";
describe("capacity planning",()=>{it("uses UTC Monday weeks and immutable effective-dated inputs",()=>{expect(weekKey(new Date("2026-09-17T23:00:00-07:00")).toISOString()).toBe("2026-09-14T00:00:00.000Z");const from=new Date("2026-09-14T00:00:00Z"),team={id:"t",name:"Platform",members:[{userId:"u",startsAt:new Date("2026-01-01"),endsAt:null,weeklyCapacityPoints:null}],capacities:[{effectiveFrom:new Date("2026-01-01"),weeklyCapacityPoints:10},{effectiveFrom:new Date("2026-09-21"),weeklyCapacityPoints:20}],availability:[],holidays:[{date:new Date("2026-09-16"),capacityReductionPoints:2}]},weeks=calculateCapacity({from,weeks:2,teams:[team],allocations:[{teamId:"t",weekStart:from,estimatePoints:12}],unassignedPoints:3});expect(weeks[0]).toMatchObject({totalCapacity:8,totalAllocated:12,unassignedPoints:3,teams:[{remaining:-4,overAllocated:true}]});expect(weeks[1].totalCapacity).toBe(20)});it("keeps individual details out of aggregate rows",()=>{const result=calculateCapacity({from:new Date("2026-09-14"),weeks:1,teams:[{id:"t",name:"Private",members:[{userId:"secret-user",startsAt:new Date("2026-01-01"),endsAt:null,weeklyCapacityPoints:5}],capacities:[],availability:[],holidays:[]}],allocations:[],unassignedPoints:0});expect(JSON.stringify(result)).not.toContain("secret-user")})});
