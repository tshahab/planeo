import {describe,expect,it}from"vitest";import{accessibleProjectWhere}from"@/lib/project-query";import type{AuthContext}from"@/lib/auth";
const context=(role:AuthContext["role"]):AuthContext=>({role,user:{id:"u1",email:"u@example.com",name:"User",avatarUrl:null,timezone:"UTC",emailNotifications:true,inAppNotifications:true},workspace:{id:"w1",name:"One",slug:"one"}});
describe("project authorization scope",()=>{it("always binds queries to the current tenant",()=>expect(accessibleProjectWhere(context("OWNER"))).toMatchObject({workspaceId:"w1",archivedAt:null}));it("limits regular members to public or explicitly assigned projects",()=>expect(accessibleProjectWhere(context("MEMBER"))).toMatchObject({workspaceId:"w1",OR:[{visibility:"PUBLIC"},{members:{some:{userId:"u1"}}}]}))});
