"use client";import{useState}from"react";
export function PortalProfileForm({workspace,profile}:{workspace:string;profile:{name:string;email:string;locale:string;emailNotifications:boolean}}){const[state,setState]=useState(profile),[message,setMessage]=useState("");async function submit(e:React.FormEvent){e.preventDefault();setMessage("");const response=await fetch(`/api/portal/${workspace}/profile`,{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify(state)});const body=await response.json();setMessage(response.ok?"Profile saved.":body.error)}return <form onSubmit={submit}><label>Name<input value={state.name} maxLength={100} onChange={e=>setState({...state,name:e.target.value})}/></label><label>Email<input value={state.email} disabled/></label><label>Language<select value={state.locale} onChange={e=>setState({...state,locale:e.target.value})}><option value="en">English</option><option value="es">Español</option><option value="fr">Français</option><option value="de">Deutsch</option></select></label><label><input type="checkbox" checked={state.emailNotifications} onChange={e=>setState({...state,emailNotifications:e.target.checked})}/> Email notifications</label><button>Save profile</button>{message&&<p role="status">{message}</p>}</form>}
