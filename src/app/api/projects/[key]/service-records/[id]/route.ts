import { NextResponse } from "next/server"; import { getAuthContext } from "@/lib/auth"; import { db } from "@/lib/db"; import { queueProject } from "@/lib/service-queues"; import { requireProjectPermission } from "@/lib/permissions";
export async function PATCH(request:Request,{params}:{params:Promise<{key:string;id:string}>}){const c=await getAuthContext();if(!c)return NextResponse.json({error:"Authentication required."},{status:401});const {key,id}=await params;const p=await queueProject(c,key).catch(()=>null);if(!p||!await requireProjectPermission(c,p.id,"issue.edit"))return NextResponse.json({error:"Not found."},{status:404});const b=await request.json();const r=await db.serviceRecord.updateMany({where:{id,workspaceId:c.workspace.id,projectId:p.id},data:{...(typeof b.status==="string"?{status:b.status.slice(0,40)}:{}),...(typeof b.details==="object"&&b.details?{details:b.details}:{})}});if(!r.count)return NextResponse.json({error:"Not found."},{status:404});return NextResponse.json({ok:true});}
