import { NextResponse } from "next/server"; import { getAuthContext } from "@/lib/auth"; import { db } from "@/lib/db"; import { queueProject } from "@/lib/service-queues"; import { requireProjectPermission } from "@/lib/permissions";
export async function GET(_:Request,{params}:{params:Promise<{key:string}>}){const c=await getAuthContext();if(!c)return NextResponse.json({error:"Authentication required."},{status:401});const p=await queueProject(c,(await params).key).catch(()=>null);if(!p||!await requireProjectPermission(c,p.id,"issue.view"))return NextResponse.json({error:"Project not found."},{status:404});const rows=await db.serviceRequest.findMany({where:{workspaceId:c.workspace.id,projectId:p.id},select:{createdAt:true,issue:{select:{status:{select:{category:true}}}},requestType:{select:{name:true}}},take:10000});const byType=new Map<string,{created:number;resolved:number;backlog:number}>();for(const row of rows){const x=byType.get(row.requestType.name)??{created:0,resolved:0,backlog:0};x.created++;if(row.issue.status.category==="DONE")x.resolved++;else x.backlog++;byType.set(row.requestType.name,x);}return NextResponse.json({generatedAt:new Date().toISOString(),timezone:"UTC",metrics:[...byType].map(([requestType,values])=>({requestType,...values}))});}
