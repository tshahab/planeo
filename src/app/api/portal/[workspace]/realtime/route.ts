import { NextResponse } from "next/server"; import { db } from "@/lib/db"; import { getPortalContext, portalRequestWhere } from "@/lib/portal-auth";
export async function GET(request: Request, { params }: { params: Promise<{ workspace: string }> }) { const context = await getPortalContext(); const { workspace } = await params; if (!context || context.workspace.slug !== workspace) return NextResponse.json({ error: "Portal not found." }, { status: 404 }); const since = new Date(new URL(request.url).searchParams.get("since") ?? Date.now() - 60_000); if (Number.isNaN(since.getTime())) return NextResponse.json({ error: "Invalid cursor." }, { status: 400 }); const allowed = await db.serviceRequest.findMany({ where: portalRequestWhere(context), select: { issueId: true } }); const events = await db.realtimeEvent.findMany({ where: { workspaceId: context.workspace.id, resourceId: { in: allowed.map((item) => item.issueId) }, createdAt: { gt: since } }, select: { id: true, type: true, resourceId: true, createdAt: true }, orderBy: { createdAt: "asc" }, take: 100 }); return NextResponse.json({ events, cursor: new Date().toISOString() }); }
