import { NextResponse } from "next/server";
import { hashPassword } from "@/lib/auth";
import { consumePortalToken } from "@/lib/portal-auth";
import { db } from "@/lib/db";

export async function POST(request: Request) { const body = await request.json().catch(() => null) as Record<string, unknown> | null; const token = typeof body?.token === "string" ? body.token : ""; const password = typeof body?.password === "string" ? body.password : ""; if (password.length < 12 || !/[a-z]/.test(password) || !/[A-Z]/.test(password) || !/\d/.test(password)) return NextResponse.json({ error: "Password must be at least 12 characters and include upper, lower, and numeric characters." }, { status: 400 }); const customer = await db.$transaction(async (tx) => { const found = await consumePortalToken(tx, token, "PASSWORD_RECOVERY"); if (!found) return null; await tx.portalCustomerSession.updateMany({ where: { customerId: found.id, revokedAt: null }, data: { revokedAt: new Date() } }); return tx.portalCustomer.update({ where: { id: found.id }, data: { passwordHash: hashPassword(password) }, include: { workspace: { select: { slug: true } } } }); }); if (!customer) return NextResponse.json({ error: "Recovery link is invalid or expired." }, { status: 400 }); return NextResponse.json({ workspace: customer.workspace.slug }); }
