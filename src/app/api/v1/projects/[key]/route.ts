import { apiError,authenticateApi,tokenProjectWhere } from "@/lib/api-auth";import { db } from "@/lib/db";
export async function GET(request:Request,{params}:{params:Promise<{key:string}>}){const auth=await authenticateApi(request,"projects:read");if(auth.error)return auth.error;const project=await db.project.findFirst({where:{...tokenProjectWhere(auth),key:(await params).key.toUpperCase()},select:{id:true,key:true,name:true,description:true,visibility:true,template:true,updatedAt:true}});return project?Response.json({data:project}):apiError(404,"not_found","Project not found.",auth.requestId)}
