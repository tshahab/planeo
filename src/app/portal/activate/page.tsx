import{PortalActivationForm}from"@/components/portal-activation-form";export default async function Activate({searchParams}:{searchParams:Promise<{token?:string}>}){const token=(await searchParams).token??"";return <main className="auth-page"><section><div className="auth-brand"><span>P</span>Customer portal</div><h1>Activate your account</h1>{token?<PortalActivationForm token={token}/>:<p className="login-error">Activation link is invalid or expired.</p>}</section></main>}
