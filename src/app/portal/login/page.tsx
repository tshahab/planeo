import { PortalLoginForm } from "@/components/portal-login-form";
export default async function PortalLogin({searchParams}:{searchParams:Promise<{workspace?:string}>}){const workspace=(await searchParams).workspace?.trim().toLowerCase()??"";return <main className="auth-page"><section><div className="auth-brand"><span>P</span>Customer portal</div><h1>Sign in</h1><p>Use the customer account from your invitation.</p>{workspace?<PortalLoginForm workspace={workspace}/>:<p className="login-error" role="alert">Open the sign-in link for your organization’s portal.</p>}</section></main>}
