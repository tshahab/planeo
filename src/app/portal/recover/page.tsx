import{PortalActivationForm}from"@/components/portal-activation-form";export default async function Recover({searchParams}:{searchParams:Promise<{token?:string}>}){const token=(await searchParams).token??"";return <main className="auth-page"><section><div className="auth-brand"><span>P</span>Customer portal</div><h1>Reset your password</h1>{token?<PortalActivationForm token={token} recovery/>:<p className="login-error">Recovery link is invalid or expired.</p>}</section></main>}
